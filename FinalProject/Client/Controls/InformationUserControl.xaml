<UserControl
    x:Class="Client.Controls.InformationUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Client.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="100"
    d:DesignWidth="200">
    <StackPanel>
    <ListView Name="InformationLV">
        <ListView.ItemTemplate>
            <DataTemplate>
                <Grid>
                        <TextBlock Text="{Binding FlightId}" FontSize="10" Foreground="Yellow"/>
                        <TextBlock Text="{Binding FlightHour}" FontSize="10" Foreground="Yellow"/>
                </Grid>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
    </StackPanel>
</UserControl>
