<UserControl
    x:Class="Client.Controls.FlightsDetails"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Client.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Converters="using:Client.Converters"
    mc:Ignorable="d"
    d:DesignHeight="100"
    d:DesignWidth="200" 
    Background="Black">
    
    <UserControl.Resources>
        <Converters:ActionTimeToVisibilityConverter x:Key="ActionTimeToVisibilityConverter"/>
    </UserControl.Resources>
    
    <ListView Name="InformationLV" Background="Black" Height="220">
        <ListView.ItemTemplate>
            <DataTemplate>
                <StackPanel Orientation="Horizontal" Visibility="{Binding ActionTime , Converter={StaticResource ActionTimeToVisibilityConverter} , Mode=OneWay}">
                    <TextBlock Text="{Binding Name}" FontSize="14" Foreground="Yellow" Margin="4,0,4,0"/>
                    <TextBlock Text="{Binding ActionTime}" FontSize="14" Foreground="Yellow" Margin="4,0,4,0"/>
                    <TextBlock Text="{Binding flightState}" FontSize="14" Foreground="Yellow" Margin="4,0,4,0"/>
                    </StackPanel>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</UserControl>
